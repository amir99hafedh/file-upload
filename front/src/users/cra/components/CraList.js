/*import React from "react";
//import Button from "../Admin/FormElements/Button";
//import "./CraList.css";
import CraItems from "./CraItems";

const CraList = (props) => {
  return (
    <div className="place-list">
      {props.items.map((data) => (
        <CraItems
          // key={place.id}
          id={data._id}
          date={data.selectedDate}
          presence={data.presence}
          rtt={data.rtt}
          congeSansSolde={data.congeSansSolde}
          absence={data.absence}
          maladie={data.maladie}
        />
      ))}
    </div>
  );
};

export default CraList; */
